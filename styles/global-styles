'use strict'

import { StyleSheet } from "react-native"

export default globalStyles = new StyleSheet.create({
    bold: {
        fontWeight: 'bold'
    },

    italic: {
        fontStyle: 'italic'
    },
    
    underline: {
        textDecorationLine: 'underline'
    }
})